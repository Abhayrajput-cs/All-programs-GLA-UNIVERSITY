#include<stdio.h>
#include<math.h>
int main() 
{
int sum=0,sum1=0,b,b11,c;
	 static int a;
	scanf("%d",&a);
	for(int i=a;i>=1;i=i/10)
	{
		b=a%10;
		sum=sum+b;
	} 
	int n=log10(sum)+1;
	if(n==1)
	printf("%d",sum);
	else if
		for(int i=sum;i>=1;i=i/10)
	{
		b1=sum%10;
		sum1=sum1+b1;
	} 
	else 
	goto n:

	
return 0;   
}
